# Environment variables for DrawPin.
#
# Copy this file to `.env.local` (git-ignored) and fill in real values.
# No variables are consumed by the app yet — this file is a placeholder that
# will be populated as Supabase, OpenAI moderation, Turnstile, and
# FingerprintJS are wired in (see docs/PLAN.md and the tech stack table in
# CLAUDE.md). Every variable added to the app must be listed here, per the
# code-documentation skill.
