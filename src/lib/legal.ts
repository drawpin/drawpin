/**
 * Where people reach us about their data.
 *
 * Google's OAuth consent screen and the privacy policy both publish this, so
 * it has to be an address someone actually reads. Namecheap forwards mail on
 * a domain for free, which keeps a personal inbox off a public page.
 */
export const CONTACT_EMAIL = "hello@drawpin.io";

/** Shown on the policy pages, so people can see whether anything moved. */
export const POLICIES_UPDATED = "21 September 2026";
